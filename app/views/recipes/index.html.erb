<%= render 'shared/navbar' %>

<div class = "container content mt-5 pb-6">
  <h1> Welcome to your Recipes!</h1>
    <% if @recipes.empty? %>

    <h3 class="">You have no Recipes yet!</h3>
    <p class="">Try adding from the Add recipe food button below</p>

    <% else %>
      <% @recipes.each do |recipe| %>
      <div class="recipes columns mt-4">
          <div class="column">
            <h2><%= recipe.name %></h2>
            <% if can? :destroy ,recipe %>
              <%= link_to  "DETAILS", recipe_path(recipe), class:"mt-2" %>
              <%= button_to "REMOVE", recipe, method: :delete, class:"button is-danger is-small mt-2" %></td>
            <% end %>
          </div>
          <div class="description column mr-6">
            <p class=""><%= recipe.description %></p>
          </div> 
      </div>
      <% end %>
      <% end %>
      <%= link_to  "Add a recipe", new_recipe_path, class:"button is-primary" %>
</div>
